extends layout

block content
  h1 All Tasks
  ul
    each task in tasks
      li 
        a(href= "/tasks/" + task.id)
          h3 #{task.title} 
            span 
              em #{task.status}
        p #{task.description}